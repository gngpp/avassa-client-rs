name: temperature
applications:
  - name: temp-collector
    placement:
      match_dc_labels: >
        type = edge
  - name: temp-aggregator
    placement:
      match_dc_labels: >
        type = top

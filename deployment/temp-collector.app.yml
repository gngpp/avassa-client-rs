name: temp-collector
services:
  - name: collector
    containers:
      - name: collector
        image: "temperature"
        cmd: ["collector"]
        env:
          - name: RUST_LOG
            value: "debug"
          - name: SUPD
            value: "http://api:4646"
    mode: replicated
    replicas: 1
 
